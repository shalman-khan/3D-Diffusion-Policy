name: real_cable_pull

task_name: RealCablePull

shape_meta: &shape_meta
  obs:
    point_cloud:
      shape: [1024, 6]   # XYZRGB — zarr stores 6 channels; use_pc_color controls whether RGB is used
      type: point_cloud
    agent_pos:
      # robot1_joints(6) + gripper1(1) + robot2_joints(6) + gripper2(1) = 14
      shape: [14]
      type: low_dim
  action:
    # absolute joint positions: robot1(6) + gripper1(1) + robot2(6) + gripper2(1) = 14
    shape: [14]

dataset:
  _target_: diffusion_policy_3d.dataset.robosuite_dataset.RobosuiteDataset
  zarr_path: /home/rosi/3D-Diffusion-Policy/3D-Diffusion-Policy/data/rgb_real_cable_pull_trimmed_clean.zarr
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.1
  max_train_episodes: null

# No sim rollouts for real robot — evaluation is done via policy_executor.py
env_runner:
  _target_: diffusion_policy_3d.env_runner.real_robot_runner.RealRobotRunner
